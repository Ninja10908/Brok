local Pl = game:GetService("Players")
local Me = Pl.LocalPlayer
local c = nil
local hrp = nil
local sg = nil
local ubButton = nil

local function pmf(i)
    local p = i
    while p and p ~= workspace do
        if p.Parent and p.Parent.Name == "Plots" and p:IsA("Model") then 
            return p 
        end
        p = p.Parent
    end
end

local function fcp()
    if not hrp then return nil end
    local cf = hrp.CFrame
    local sz = Vector3.new(100,50,100)
    local pr = OverlapParams.new()
    pr.FilterType = Enum.RaycastFilterType.Exclude
    pr.FilterDescendantsInstances = {c}
    local fp = {}
    for _,pt in ipairs(workspace:GetPartBoundsInBox(cf,sz,pr)) do
        local m = pmf(pt)
        if m then
            fp[m] = true
        end
    end
    local cl,cd = nil,math.huge
    for pl in pairs(fp) do
        local pv = pl:GetPivot().Position
        local ds = (pv - hrp.Position).Magnitude
        if ds < cd then
            cl,cd = pl,ds
        end
    end
    return cl
end

local function ifl(pp,ur)
    local t = ((pp.ActionText or "").." "..(pp.ObjectText or "")):lower()
    if t:match("first") or t:match("1st") then return 1 end
    if t:match("second") or t:match("2nd") then return 2 end
    if t:match("third") or t:match("3rd") then return 3 end
    local cr = pp
    while cr and cr ~= ur do
        local f = cr:GetAttribute("Floor")
        if f ~= nil then return tonumber(f) end
        cr = cr.Parent
    end
end

local function cup(pl)
    local ur = pl and pl:FindFirstChild("Unlock",true)
    if not ur then return {} end
    local fd = {}
    for _,pp in ipairs(ur:GetDescendants()) do
        if pp:IsA("ProximityPrompt") then
            local at = (pp.ActionText or ""):lower()
            local ot = (pp.ObjectText or ""):lower()
            if at:match("unlock") or ot:match("unlock") then
                local n = ifl(pp,ur)
                if n and n >= 1 and n <= 3 then
                    fd[n] = pp
                end
            end
        end
    end
    return fd
end

local function fp(p)
    if not p or not p.Parent then
        return false
    end
    if fireproximityprompt then
        fireproximityprompt(p)
        return true
    end
    local fr = getfenv().fireproximityprompt or getgenv and getgenv().fireproximityprompt
    if fr then
        fr(p)
        return true
    end
    local od = p.MaxActivationDistance
    local ol = p.RequiresLineOfSight
    local oh = p.HoldDuration
    pcall(function()
        p.MaxActivationDistance = 9999
        p.RequiresLineOfSight = false
        p.HoldDuration = 0
        p.Enabled = true
    end)
    local s = pcall(function()
        p:InputHoldBegin()
        task.wait(0.05)
        p:InputHoldEnd()
    end)
    task.delay(0.1,function()
        pcall(function()
            p.MaxActivationDistance = od
            p.RequiresLineOfSight = ol
            p.HoldDuration = oh
        end)
    end)
    return s
end

local function gfy()
    if not hrp or not hrp.Parent then return nil end
    local yp = hrp.Position.Y
    if yp >= -7 and yp <= 0 then
        return 1
    elseif yp >= 5 and yp <= 20 then
        return 2
    elseif yp >= 23 and yp <= 40 then
        return 3
    end
    return nil
end

local function ucf()
    if not hrp or not hrp.Parent then
        c = Me.Character or Me.CharacterAdded:Wait()
        hrp = c:WaitForChild("HumanoidRootPart")
    end
    
    local cf = gfy()
    if not cf then return false end
    
    local plot = fcp()
    if not plot then return false end
    
    local prs = cup(plot)
    local p = prs[cf]
    if not p then return false end
    
    local s = fp(p)
    return s
end

sg = Instance.new("ScreenGui")
sg.Name = "UnlockGui"
sg.ResetOnSpawn = false
sg.Parent = Me:WaitForChild("PlayerGui")

ubButton = Instance.new("TextButton")
ubButton.Name = "UnlockButton"
ubButton.Text = "Unlock"
ubButton.Size = UDim2.new(0,60,0,30)
ubButton.Position = UDim2.new(1,-95,0.5,-130)
ubButton.AnchorPoint = Vector2.new(1,0.5)
ubButton.BackgroundColor3 = Color3.new(0,0,0)
ubButton.TextColor3 = Color3.new(1,1,1)
ubButton.Font = Enum.Font.GothamBold
ubButton.TextSize = 14
ubButton.Parent = sg

Instance.new("UICorner",ubButton).CornerRadius = UDim.new(0,6)

ubButton.MouseButton1Click:Connect(function()
    ucf()
end)

_G.cleanupUnlock = function()
    if sg and sg.Parent then
        sg:Destroy()
        sg = nil
    end
    ubButton = nil
end
